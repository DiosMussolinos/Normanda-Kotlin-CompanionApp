<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav"
    app:startDestination="@id/stats">

    <fragment
        android:id="@+id/mapFragment"
        android:name="com.example.normanda.MapFragment"
        android:label="fragment_map"
        tools:layout="@layout/fragment_map" />
    <fragment
        android:id="@+id/wiki"
        android:name="com.example.normanda.Wiki"
        android:label="fragment_wiki"
        tools:layout="@layout/fragment_wiki" />
    <fragment
        android:id="@+id/challengeFragment"
        android:name="com.example.normanda.ChallengeFragment"
        android:label="fragment_challenge"
        tools:layout="@layout/fragment_challenge" />
    <fragment
        android:id="@+id/stats"
        android:name="com.example.normanda.Stats"
        android:label="fragment_stats"
        tools:layout="@layout/fragment_stats" />
</navigation>